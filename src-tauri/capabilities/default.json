{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the Hermes Mobile window.",
  "windows": ["main"],
  "permissions": ["core:default", "websocket:default"]
}
